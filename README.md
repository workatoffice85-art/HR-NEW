# HR DEMO

Supabase backend starter is available.

See: `backend/SUPABASE_MIGRATION.md`

## Frontend Config

When using Supabase Edge Functions, set both values in `assets/app-config.js`:

- `API_URL`
- `SUPABASE_ANON_KEY` (from Supabase Project Settings → API)
